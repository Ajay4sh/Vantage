{
  "name": "vantage",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "lint": "npm run lint -w web",
    "test": "npm run test -w web",
    "refresh-token": "npm run refresh-token -w web"
  }
}
